export interface Project {
    updatedAt: any;
    idProjet: number;
    nomProjet: string;
    descriptionProjet: string;
    dateDebut: string;  // Or use Date if you want to parse it correctly
    dateFin: string;    // Or use Date
    etatProjet: string;
  }
  